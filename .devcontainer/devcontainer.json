{
  "name": "PHP & PostgreSQL Dev Container",
  "dockerComposeFile": "docker-compose.yml",
  "service": "app", // The name of your PHP service in docker-compose.yml
  "workspaceFolder": "/workspace",
  "extensions": [
    "ms-vscode.php-pack", // Example PHP extension pack
    "ms-azuretools.vscode-docker",
    "ms-vscode-remote.remote-containers"
  ],
  "forwardPorts": [80, 5432],
  "postCreateCommand": "sudo chmod a+x \"$(pwd)\" && sudo rm -rf /var/www/html && sudo ln -s \"$(pwd)\" /var/www/html && sudo psql postgresql://user:password@127.0.0.1:5432/article -f .devcontainer/setup-postgresql.sql"
}
